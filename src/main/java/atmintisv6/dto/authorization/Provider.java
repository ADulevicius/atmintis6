package atmintisv6.dto.authorization;

/**
 * Enum representing different authentication providers.
 */
public enum Provider {
    LOCAL,    // Local authentication provider
    GOOGLE,   // Google authentication provider
    GITHUB    // GitHub authentication provider
}
